package com.company;

public class Strings {
    public static void main(String[] args){
        // Strings || \u0000 for unicode || immutable once created
        String myString = "This is a string";
        System.out.format("my string is is equal to %s", myString);
    }
}
